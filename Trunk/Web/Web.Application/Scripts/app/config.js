define('config',['ko'],
    function (ko) {
        
        var viewIds = {
            header: '#master-header',
            footer: '#master-footer'
        };

        return {
            viewIds: viewIds
        };

    });
